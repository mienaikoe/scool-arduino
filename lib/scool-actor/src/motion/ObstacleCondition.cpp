
#include "ObstacleCondition.h"

namespace Scool {
  namespace Core {

    bool ObstacleCondition::checkAndHandle(){
      // check sensor
      // if no obstacle, return true
      // if obstacle, run rotate routine until done

      return true;
    }

  }
}